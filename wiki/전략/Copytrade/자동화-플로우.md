---
title: "Copytrade 자동화 플로우"
category: "전략"
created: "2026-04-05"
sources:
  - url: "https://github.com/Fabianhvandijk/polymarket-copytrade"
    raw: "raw/fetched/2026-04-07-leaderboard-and-risk.md"
    added: "2026-04-07"
  - url: "https://github.com/agc-desarrollo/polymarket-trading-bot"
    raw: "raw/fetched/2026-04-06-copytrade-sources.md"
    added: "2026-04-06"
  - url: "https://gamma-api.polymarket.com/events"
    raw: "raw/fetched/2026-04-07-negative-risk-and-api.md"
    added: "2026-04-07"
  - url: "https://docs.polymarket.com/api-reference/clients-sdks"
    raw: "raw/fetched/2026-04-07-negative-risk-and-api.md"
    added: "2026-04-07"
  - url: "https://polycopy.app/discover"
    raw: "raw/fetched/2026-04-07-leaderboard-and-risk.md"
    added: "2026-04-07"
related:
  - "[[개념/Copytrade]]"
  - "[[전략/Copytrade/Copytrade-성능-모니터링]]"
  - "[[트레이더/리더보드/데이터]]"
  - "[[기술/API/Polymarket-API-레퍼런스]]"
  - "[[개념/CLOB]]"
  - "[[전략/Copytrade/Copytrade-리더보드-자동화]]"
  - "[[전략/Copytrade/리스크-모니터링]]"
  - "[[개념/Copytrade#백테스트 설계 (권장)]]"
---

# Copytrade 자동화 플로우

> 리더보드 기반 copytrade의 자동화 설계와 모니터링 체크리스트

## 내용

Copytrade 자동화는 크게 1) 트레이더 선별, 2) 포지션 동기화, 3) 리스크 제어, 4) 모니터링·알림의 네 단계로 구성된다.

## 아키텍처 개요

```
┌─────────────────┐    ┌──────────────────┐    ┌───────────────────┐
│  Leaderboard     │    │  Polymarket CLOB │    │  On-chain Data    │
│  Snapshot Engine │───▶│  WebSocket Feed  │◀───│  (Polygonscan)    │
└────────┬────────┘    └────────┬─────────┘    └────────┬──────────┘
         │                      │                       │
         ▼                      ▼                       ▼
┌─────────────────┐    ┌──────────────────┐    ┌───────────────────┐
│  Trader Screen  │    │  Signal Normalizer│    │  Position Tracker │
│  & Scoring      │───▶│  (Entry/Exit)     │───▶│  Reconciliation   │
└────────┬────────┘    └────────┬─────────┘    └────────┬──────────┘
         │                      │                       │
         ▼                      ▼                       ▼
┌─────────────────┐    ┌──────────────────┐    ┌───────────────────┐
│  Weight Engine  │    │  Order Executor   │    │  Risk Monitor     │
│  (Kelly/Equal)  │───▶│  (CLOB API)       │    │  & Alert System   │
└─────────────────┘    └──────────────────┘    └───────────────────┘
```

1) 트레이더 선별
- 리더보드 및 과거 성과로 후보군을 만들고, 승률· 평균 포지션 크기· 최대 드로우다운을 기준으로 필터링한다. (관련: [[전략/Copytrade/팔로우-선별]], [[전략/Copytrade/알고리즘-선별-체크리스트]])
- Polycopy 등 서드파티 리더보드(Polycopy: 500K+ 트레이더)를 교차 검증해 단일 플랫폼 편향을 피한다.
- Gamma API `/events` 엔드포인트에서 `volume`, `liquidity`, `rewardsMaxSpread` 필드를 읽어 유동성이 충분한 마켓만 타겟팅한다. 유동성 임계치 미달 마켓은 자동 제외.

2) 포지션 동기화
- 선택된 트레이더의 온체인/오프체인 포지션을 감지하여, 신호(진입/청산)를 표준화한 후 사용자 포트폴리오에 반영한다. WebSocket(체결 스트림)과 Data API(포지션 스냅샷)를 병행 사용한다. (관련: [[기술/API/Polymarket-API-레퍼런스]], [[기술/WebSocket-실시간데이터]])
- Neg-Risk 마켓(`negRisk: true`)은 CTF 마켓과 주문 구조가 다르므로 `NegRiskAdapter` 컨트랙트 경로로 별도 처리한다.
- 신호 중복 방지: 동일 이벤트 ID에 대해 5초 내 중복 신호가 들어오면 디바운스(debounce) 처리.

3) 리스크 제어
- 포트폴리오별 최대 노출 한도, 시장별 비중, 슬리피지 허용치, 자동 롤백 조건을 설정한다. 트레이더의 레버리지·포지션 사이즈가 급격히 증가하면 자동으로 동기화 비율을 낮춘다. 정량 기준(예: 24h 변동성, 최근 10거래의 평균 수익률)으로 트레이더 가중치를 재계산한다.
- 서킷 브레이커: 단일 트레이더 24h 손실 > 설정값(예: -5%) → 모든 팔로우 일시 중지, 기존 포지션은 유지.
- 상대적 비중 조정: Kelly Criterion 또는 Equal Weight 중 선택. 자동화 파이프라인에서는 기본적으로 Equal Weight로 시작, Kelly分数 계산을 통해 점진적 전환을 권장한다.

4) 모니터링·알림
- 실시간 체결 실패, API 오류, 급격한 포지션 변화 발생 시 알림을 전송한다. 성능 모니터링 지표로는 복제 정확도(원본 대비 포지션 차이 비율), 지연(latency), 비용(거래비용+슬리피지) 등을 기록한다. (관련: [[전략/Copytrade/Copytrade-성능-모니터링]], [[전략/Copytrade/리스크-모니터링]])
- 상태 헬스체크: 매 30초마다 `ping()`으로 CLOB WebSocket 연결 상태 확인, 끊어지면 3번 재시도 후 Slack/Webhook 알림.

## Python 자동화 파이프라인 (의사코드)

```python
class CopytradePipeline:
    def __init__(self, config):
        self.clob_client = CLOBClient(config.api_key)     # py-clob-client
        self.ws = WebSocketFeed("wss://ws-api.polymarket.com")
        self.risk_monitor = RiskMonitor(max_drawdown=config.max_dd)
        self.circuit_breaker = CircuitBreaker(threshold=0.05)  # 5% daily loss

    async def run(self):
        await self.ws.connect()
        self.ws.on("trade", self.handle_trade_signal)
        self.ws.on("error", self.handle_ws_error)

        # Periodic leader 동기화 (Leaderboard poll every 5min)
        while True:
            traders = await self.fetch_leaderboard_scores()
            weights = self.calculate_weights(traders)
            await self.rebalance_positions(weights)
            await asyncio.sleep(300)

    async def handle_trade_signal(self, trade):
        if self.circuit_breaker.is_open():
            log.warn("Circuit breaker open — skipping signal")
            return

        normalized = self.normalize_signal(trade)
        if not self.risk_monitor.check(normalized):
            log.warn(f"Risk check failed for {normalized.event_id}")
            return

        await self.execute_order(normalized)

    def calculate_weights(self, traders):
        # Equal weight fallback → Kelly as data accumulates
        if len(traders) == 0:
            return {}
        base = 1.0 / len(traders)
        return {t.wallet: base for t in traders}

if __name__ == "__main__":
    # 백테스트: [[개념/Copytrade#백테스트 설계 (권장)]] 참조
    pipeline = CopytradePipeline(config)
    asyncio.run(pipeline.run())
```

## 재연결 · 내구성 가이드

| 시나리오 | 재시도 전략 | 알림 |
|----------|------------|------|
| WebSocket disconnect | exponential backoff (1s → 2s → 4s, max 32s) | 3회 초과 시 웹훅 |
| CLOB API rate limit (429) | Retry-After 헤더 따름, max 5회 | 5회 초과 시 이메일 |
| 포지션 불일치 (on-chain mismatch) | 풀 스냅샷 재동기화 | 즉시 슬랙 알림 |
| Neg-Risk 마켓 변경 알림 | Gamma API `/notifications` 폴링 (1분 주기) | 즉시 |

## 체크리스트

- [ ] 리더보드 스냅샷 수집 자동화 (PolyCopy/PredictEngine 교차)
- [ ] WebSocket 재연결 및 메시지 파서 (exponential backoff)
- [ ] 부정 거래 신호 디바운스 (5초 윈도우)
- [ ] Neg-Risk vs CTF 마켓 라우팅 구분
- [ ] 서킷 브레이커 임계값 설정 및 테스트
- [ ] 동기화 비율 자동조정 모듈 (Equal → Kelly 전환)
- [ ] 이상거래 감지 및 롤백 정책
- [ ] CLOB API 키 로테이션 주기 설정 ( 권장: 월 1회)

## 관련 문서
- [[개념/Copytrade]] — Copytrade 기본 개념
- [[전략/Copytrade/Copytrade-성능-모니터링]] — 성능 지표와 대시보드
- [[트레이더/리더보드/데이터]] — 데이터 소스
- [[기술/API/Polymarket-API-레퍼런스]] — CLOB API 참조
- [[전략/Copytrade/Copytrade-리더보드-자동화]] — 리더보드 기반 트레이더 선별 자동화
- [[전략/Copytrade/팔로우-선별]] — 트레이더 선별 기준과 실무 절차
- [[전략/Copytrade/리스크-모니터링]] — 실시간 리스크 지표 정의
- [[개념/Copytrade#백테스트 설계 (권장)]] — 자동화 파이프라인 백테스트
